apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keda
  namespace: keda

spec:
  interval: 1m

  chart:
    spec:
      chart: keda
      version: "*"

      sourceRef:
        kind: HelmRepository
        name: kedacore
        namespace: flux-system
