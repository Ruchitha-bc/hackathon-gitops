apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: nginx-html
    files:
      - index.html

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
