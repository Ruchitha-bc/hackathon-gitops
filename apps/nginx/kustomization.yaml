apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default # <--- ADD THIS CRITICAL LINE

configMapGenerator:
  - name: nginx-html
    files:
      - index.html

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - scaledobject.yaml
