apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - frontend          # Points directly to your apps/frontend/ folder
  - docs              # Points directly to your apps/docs/ folder
  - backend           # Points directly to your apps/backend/ folder
  - ingress.yaml      # Points directly to your apps/ingress.yaml routing file
