apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend
  namespace: default

spec:
  replicas: 1

  selector:
    matchLabels:
      app: backend

  template:

    metadata:
      labels:
        app: backend

    spec:

      containers:

      - name: backend

        image: diyacmenezes202/backend:latest

        imagePullPolicy: Always

        ports:
        - containerPort: 5000

        env:

        - name: DB_HOST
          value: "YOUR_RDS_ENDPOINT"

        - name: DB_NAME
          value: "complaints_db"

        - name: DB_USER
          value: "postgres"

        - name: DB_PASSWORD
          value: "securepassword123"
